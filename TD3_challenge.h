#ifndef TD3_CHALLENGE_H
#define TD3_CHALLENGE_H

int gauss(mymatrix *C, int n, int k);
int inversible (mymatrix *C, int n);
int remontee_gauss(mymatrix *C, int n, int k);
float absolute (float x);

#endif
